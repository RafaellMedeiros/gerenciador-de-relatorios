NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secret (Project Settings > API > service_role). Never prefix
# with NEXT_PUBLIC_ or use it from client code — it bypasses Row Level Security.
SUPABASE_SERVICE_ROLE_KEY=
